import React from 'react';

const Admin = () => {
  return (
    <div style={{ maxWidth: 600, margin: '40px auto', padding: 20 }}>
      <h2>Admin</h2>
      <p>This page is now UI-only. No database or backend is connected.</p>
      <p>All contact functionality is displayed as frontend mockup only.</p>
    </div>
  );
};

export default Admin;