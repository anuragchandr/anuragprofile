function Header() {
    return (
      <header> {/* Change this to a standard HTML element like <header> */}
        <h1>My Portfolio</h1>
        <a href="#home">Home</a>
        <a href="#about">About</a>
        <a href="#projects">Projects</a>
        <a href="#contact">Contact</a>
      </header>
    );
}
  
export default Header;
  